# ResumeForge

## 版本规则

遵循语义化版本（SemVer）:

- **v1.0.1 / v1.0.2** — 小修：bug 修复、性能优化、UI 微调，不新增功能
- **v1.1.0 / v1.2.0** — 功能：新功能、新页面、新集成，向后兼容
- **v2.0.0** — 大版本：破坏性变更、架构重构、大规模 UI 重设计

发布流程：推送 `v*` 标签到 GitHub → Actions 自动构建 portable exe → 上传到 Releases。
